# Suggested OpenReview artifact/data statement

Artifact URL: https://anonymous.4open.science/r/Latent-Space-Sampling-BD8C/

This submission does not introduce or redistribute a new static dataset. The artifact release contains code, prompts, configuration files, metadata, and evaluation scripts for reproducing the MEDALS benchmark-generation and evaluation pipeline. The experiments use existing public datasets as external sources, including PubMedQA, MedQuAD, and the Kaggle Questions vs Statements Classification dataset. These datasets are not claimed as new contributions and are not re-hosted in the artifact package. The code provides instructions for obtaining the source datasets from their original public URLs and for preprocessing them.

Because the submission uses existing public datasets and is framed as a benchmark-generation/evaluation-method artifact, the anonymous artifact URL hosts code and metadata rather than a newly introduced dataset. If the submission form requires a data URL, the same anonymous artifact URL can be provided with the note that it contains code, prompts, metadata, and source-dataset references, not a new dataset release.
